setup:
#  addons:
    #- plan: heroku-redis
    #  as: REDIS
  config:
    APP_NAME: quado-dockerize
build:
  docker:
    web: backend/Dockerfile
  config:
    NODE_ENV: production
#run:
#  web: npm start
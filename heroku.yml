setup:
#  addons:
    #- plan: heroku-redis
    #  as: REDIS
  config:
    APP_NAME: quado-dockerize
build:
  docker:
    quado-backend: backend/Dockerfile
    quado-frontend: frontend/Dockerfile
  config:
    NODE_ENV: production
#run:
#  web: npm start